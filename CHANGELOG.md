# Changelog

## v2.0.1

提问作答与回执修复。

- **提问支持自由输入**：QQ 里直接打字就能作答（走 DSH 作答协议的 `answers[].custom`），不再只认数字/按钮；
  多问题拆成「（1/2）（2/2）」逐问，答完一次性回传。
- **修「点按钮没反馈」**：审批/提问的回执此前不带 `msg_id`，退化成主动消息 —— 额度用尽时被 QQ 直接丢弃，
  而且失败是静默的；现在回执复用会话的被动窗口（每个 `msg_id` 各分配唯一 `msg_seq`），失败记 `<what> failed to send`。
- **修队列误报**：忙时「并入等待」的消息在合并轮送达后没人清队列，导致每次启动都误报「有待补发消息」；
  启动时另会清掉「来源身份重算过、永远补发不到」的僵尸记录（丢弃必须留痕）。
- 提问草稿随 sweep 回收（问了没人答不再留在内存）；文本作答从 `handlers/qq.js` 抽到 `handlers/text-answer.js`。
- L1 测试 130 例全绿（`node --test --test-isolation=none`）。

## v2.0.0

相对 v1.2.x 的从零重写：来源→会话身份改为 UUIDv5 **推导**（重启复用同一对话）、实例锁、
`session/follow` 主事件源 + 分页对账、审批 id 配对、被动回复窗口语义、凭据卫生、
2093 行单文件 → 28 个模块（每个 ≤400 行）。
