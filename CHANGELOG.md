# Changelog

## v2.0.3

QQ 里的斜杠命令不再"假装执行"。

- **命令通道**：QQ 消息若以 `/命令` 开头（判据与宿主 `dsh-commands.parseCommand` 逐字一致），改走宿主
  `commands/execute` —— `/compact` 压缩的才是电脑上这个会话，`/goal`、`/plan`、`/feedback`、`/permission`、
  `/export` 同理；这些命令**不进模型**，结果（成功/失败原文）回给 QQ。
  此前插件只有 `session/prompt` 一条路，`/compact` 只是一句普通文字，模型看不懂、压缩从未发生。
- **不认识的行原样退回 prompt**：宿主对"语法不是命令 / 命令名没注册"返回**无 `value`** 的 `{ok:true}`
  （真机 0.1.7-alpha.1 核证），插件据此判"不是命令"→ 照常当普通消息发给 agent，升级前后行为一致，
  不会把用户打的普通文本吞掉（`/Compact`、`/紧凑`、`/compact后`、句子中间的 `/compact` 都不是命令）。
- **带附件的消息不走命令通道**（命令通道没有附件接收面）；命令失败（例如 agent 不空闲导致
  `/compact` 被拒）如实回给用户原文，**不自动重试**。
- v1 宿主（DSH ≤0.1.1）没有该服务 → 按"宿主没有这条通道"退回 prompt，不报假错。
- 新增 9 条单元用例（T-C0a…T-C0i，命令判据/渲染/降级）+ 4 条集成用例（T-C1 命令真执行且不进模型、
  T-C2 未注册行退回 prompt、T-C2b 非命令行不碰通道、T-C3 命令报错如实回执）；去掉修复即红。L1 = 147/147。
- 协议文档补 §6.5 `commands/execute`（请求 args 逐字、无 `value` 的返回形状、v1 无此服务）。

## v2.0.2

为 **DSH 0.1.7-alpha.1** 做的前置兼容（真 0.1.7 独立 DSH_HOME 预飞通过；0.1.5/0.1.6 行为不变）。

- **设置接口兼容"第三代宿主"**：0.1.7 移除了 `dsh-settings.installSection`（插件配置改由 plugin-manager +
  profile entry 持有，设置面板按插件的 `Config` schema 渲染）。插件识别该形态后直接采用 profile entry
  配置，不再空转 24 次注册重试、也不再报"设置注册耗尽"；0.1.5/0.1.6 仍走原来的 `installSection` 路径。
- **自举重试**：宿主服务（`webServer`/`connection`）在 `apply()` 之后约 1s 才就绪。0.1.6 靠"settings 注册
  成功 → 生效配置变了 → 重建通道"顺手救回，而 0.1.7 配置由宿主托管、不再触发重建 —— 一旦抢跑，QQ 通道会
  一直哑到下次重启。现在插件自己按 1s 间隔、最多 15 次重试自举（`BOOT_RETRY_MS` / `BOOT_RETRY_MAX`），
  耗尽时显式报错而不是静默。
- 与 0.1.7 其它改动的核对结论：插件 config 仍按普通值传入（只有标 `.volatile()` 的字段才变 store）、
  `ctx.get('webServer'|'connection')`、`session/*` + `$events` + `remote.mux` 契约、事件名（`turn/start`、
  `assistant/message`…）均未变；会话日志 v3→v4 不影响本插件（走 API、不读日志文件）。
- 新增两条集成用例：**T-R1**（宿主服务晚就绪必须自行重试到通道建立）、**T-R2**（宿主托管设置下通道照常建立），
  去掉修复即红。L1 = 134/134。

## v2.0.1

提问作答与回执修复。

- **提问支持自由输入**：QQ 里直接打字就能作答（走 DSH 作答协议的 `answers[].custom`），不再只认数字/按钮；
  多问题拆成「（1/2）（2/2）」逐问，答完一次性回传。
- **修「点按钮没反馈」**：审批/提问的回执此前不带 `msg_id`，退化成主动消息 —— 额度用尽时被 QQ 直接丢弃，
  而且失败是静默的；现在回执复用会话的被动窗口（每个 `msg_id` 各分配唯一 `msg_seq`），失败记 `<what> failed to send`。
- **修队列误报**：忙时「并入等待」的消息在合并轮送达后没人清队列，导致每次启动都误报「有待补发消息」；
  启动时另会清掉「来源身份重算过、永远补发不到」的僵尸记录（丢弃必须留痕）。
- 提问草稿随 sweep 回收（问了没人答不再留在内存）；文本作答从 `handlers/qq.js` 抽到 `handlers/text-answer.js`。
- L1 测试 130 例全绿（`node --test --test-isolation=none`）。

## v2.0.0

相对 v1.2.x 的从零重写：来源→会话身份改为 UUIDv5 **推导**（重启复用同一对话）、实例锁、
`session/follow` 主事件源 + 分页对账、审批 id 配对、被动回复窗口语义、凭据卫生、
2093 行单文件 → 28 个模块（每个 ≤400 行）。
